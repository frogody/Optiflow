{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "installCommand": "if [ -f 'pnpm-lock.yaml' ]; then pnpm install --no-frozen-lockfile; else npm install --no-frozen-lockfile; fi",
  "framework": "nextjs",
  "github": {
    "enabled": true,
    "silent": true
  },
  "env": {
    "NEXT_PUBLIC_DEPLOYMENT_ENV": "production",
    "NEXT_PUBLIC_VERCEL_ENV": "production"
  }
} 